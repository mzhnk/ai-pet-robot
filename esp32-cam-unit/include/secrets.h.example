// Developer-local secrets for the ESP32-CAM unit.
// Copy this file to include/secrets.h (git-ignored) and fill in real values.
#pragma once

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Main robot's IP address on the same network (vision link target).
#define ROBOT_HOST    "192.168.1.20"
