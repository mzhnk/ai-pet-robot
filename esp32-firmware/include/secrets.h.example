/**
 * secrets.h.example — Copy this file to include/secrets.h and fill in
 * your local values. secrets.h is git-ignored on purpose.
 */
#pragma once

#define WIFI_SSID      "NAMA_WIFI_KAMU"
#define WIFI_PASSWORD  "PASSWORD_WIFI_KAMU"

// IP laptop yang menjalankan Local AI (Flask) di jaringan yang sama
#define AI_WS_HOST     "192.168.1.100"
